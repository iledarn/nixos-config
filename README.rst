NixOS configuration for ``gram990``.

Rebuild the active system with:

::

 sudo nixos-rebuild switch --flake .#gram990

Switch into the Hyprland specialisation with:

::

 sudo /run/current-system/specialisation/hyprland/bin/switch-to-configuration switch
